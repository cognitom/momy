machine:
  timezone: Asia/Tokyo
  node:
    version: 4

dependencies:
  pre:
    - sudo /etc/init.d/mongodb stop

database:
  override:
    - mysql -u ubuntu -e "create database momy"

test:
  post:
    - bash <(curl -s https://codecov.io/bash)
